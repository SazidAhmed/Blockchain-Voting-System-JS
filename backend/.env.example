# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=voting

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=change_this_secret_key_in_production_use_long_random_string

# Server Configuration
PORT=3000
NODE_ENV=development

# Blockchain Node
BLOCKCHAIN_NODE_URL=http://localhost:3001

# Session Configuration
SESSION_SECRET=change_this_session_secret_in_production

# MFA Configuration (Optional)
MFA_ISSUER=University Voting System

# Encryption Keys (CHANGE THESE IN PRODUCTION!)
ENCRYPTION_KEY=32_byte_hex_key_for_encrypting_sensitive_data_change_in_prod
ENCRYPTION_IV=16_byte_hex_initialization_vector_change_in_production

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# IdP Configuration (Future)
# IDP_URL=https://university-idp.edu
# IDP_CLIENT_ID=
# IDP_CLIENT_SECRET=

# Vault/HSM Configuration (Production)
# VAULT_ADDR=https://vault.university.edu:8200
# VAULT_TOKEN=
# HSM_SLOT=0
# HSM_PIN=
